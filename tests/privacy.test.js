import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('document blocks data connections, forms, embedded objects and referrers',()=>{const html=fs.readFileSync('index.html','utf8');for(const expected of ["connect-src 'none'","form-action 'none'","base-uri 'none'","object-src 'none'",'content="no-referrer"'])assert.ok(html.includes(expected));assert.ok(html.indexOf('boot.js')<html.indexOf('styles.css'));});
test('client has no data-upload APIs or third-party resources',()=>{for(const file of ['app.js','core.js','store.js','boot.js','import-worker.js']){const content=fs.readFileSync(file,'utf8');assert.doesNotMatch(content,/\bfetch\s*\(|XMLHttpRequest|sendBeacon|WebSocket|EventSource/);}const html=fs.readFileSync('index.html','utf8');assert.doesNotMatch(html,/(?:src|href)="https?:\/\//);});
test('worker caches a fixed public allowlist and clears queries before requests',()=>{const sw=fs.readFileSync('sw.js','utf8');assert.ok(sw.includes("url.search=''"));assert.ok(sw.includes('ALLOWED.has(url.href)'));assert.doesNotMatch(sw,/fetch\(event.request\)/);assert.doesNotMatch(sw,/ledger\.txt|accounts\.json/);});
test('deployment never uploads the repository root',()=>{const workflow=fs.readFileSync('.github/workflows/pages.yml','utf8');assert.match(workflow,/path: dist/);assert.doesNotMatch(workflow,/path: \.(?:\s|$)/);});
